# Restrict package manager to pnpm only
engine-strict=true

# Prevent npm usage
prefer-offline=true

# NPM registry configuration
registry=https://registry.npmjs.org/

# Auto fix peer dependencies
auto-install-peers=true

# Public access by default for publishing
access=public
